# Create a new directory and enter it
function mkd() {
    mkdir -p "$@" && cd "$@"
}

# quickly backup a file
function backup() {
    cp -bf --suffix=.bak.`date +%Y%m%d-%H%M%S` "$1" "$1"
}

function aptinst() {
    sudo apt-get install "$@"
}

