# navigation
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
# not compatible with fish
#alias ~="cd ~"

alias rmrf='rm -rf'

# using user aliases when sudoing (does not work for functions; only aliases)
alias sudo='sudo '

alias shutdown='sudo shutdown -h -P now'

alias bak='backup'

alias cpp='cp --parent'

# concat public and private .ssh/config (as ssh doesn't provide an include feature)
alias concat_ssh_config='cat ~/.ssh/config_local ~/.ssh/config | psub'
alias ssh='ssh -F (concat_ssh_config)'
alias scp='scp -F (concat_ssh_config)'

# human readable size for several commands
alias df='df --human-readable'
alias du='du --human-readable'
alias free='free --human'

# ls
alias ls='ls -CF --human-readable --color=auto'
alias ll='ls -l'
alias la='ls -A'
alias lsa='la'
alias lla='ls -lA'

